[project]
name = "mqtt-kasa-remote"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "paho-mqtt>=2.1.0",
    "python-kasa>=0.10.2",
]
